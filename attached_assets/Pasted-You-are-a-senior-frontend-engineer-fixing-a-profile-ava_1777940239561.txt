You are a senior frontend engineer fixing a profile avatar fallback issue in a web application.

PROBLEM:
- In the sidebar, the user profile picture (pfp) is loaded from a URL
- When the image fails to load (error fetching / broken URL):
  ❌ It shows a broken image or empty state
- However, in the dashboard, the system correctly shows user initials (e.g., "JM")

EXPECTED BEHAVIOR:
- If profile image loads → show image
- If profile image fails → automatically fallback to initials (same style as dashboard)

---

TASK 1: ANALYZE CURRENT IMPLEMENTATION

1. Locate sidebar profile component
2. Find how the profile image is rendered:
   - <img src="...">
   - background-image
3. Check if there is any existing fallback logic (likely missing or inconsistent)

---

TASK 2: IMPLEMENT IMAGE FALLBACK

Add error handling:

FOR <img> TAG:
- Use onerror handler to detect failed load
- When error occurs:
  - Hide image
  - Show fallback initials container

Example logic:
- onError → set state "imageError = true"

---

TASK 3: CREATE INITIALS FALLBACK

Generate initials from user name:
- Example:
  "Jullan Carl Maglinte" → "JM"

Rules:
- Use first letter of first name + first letter of last name
- Uppercase

---

TASK 4: UI CONSISTENCY

Match the dashboard style:
- Rounded avatar
- Gradient or colored background
- Centered initials text

Ensure:
- Same size and alignment as image avatar
- Smooth fallback (no layout shift)

---

TASK 5: OPTIONAL IMPROVEMENT

If image URL is null or empty:
- Skip loading image entirely
- Directly show initials

---

EXPECTED RESULT:

- Sidebar loads:
   ✔ If image valid → show image
   ✔ If image fails → show initials (like "JM")
- No broken image icon
- UI looks consistent with dashboard

---

OUTPUT FORMAT:

1. ROOT CAUSE
2. FILES MODIFIED
3. BEFORE vs AFTER CODE
4. FALLBACK LOGIC EXPLANATION

---

IMPORTANT:
- Do NOT remove existing avatar system
- Only enhance it with fallback behavior
- Keep it reusable for other parts of the app
