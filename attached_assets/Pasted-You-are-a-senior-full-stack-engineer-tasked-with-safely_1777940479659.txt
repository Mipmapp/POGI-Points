You are a senior full-stack engineer tasked with safely removing the Terms & Conditions (T&C) system from a web application.

GOAL:
Remove ALL T&C-related features WITHOUT breaking authentication, login flow, or UI.

---

PHASE 1: DISCOVERY (DO NOT DELETE YET)

Scan the entire codebase and identify ALL T&C-related logic:

1. UI:
   - T&C modal/component
   - Acceptance checkbox
   - "I Understand & Close" button

2. Logic:
   - Flags like:
     hasAcceptedTerms
     termsAccepted
     isTCAccepted
   - Conditional rendering:
     - blocking dashboard until accepted
     - forcing modal on login

3. Backend/API:
   - Fields in database storing T&C acceptance
   - API endpoints related to terms acceptance

4. Routing:
   - Guards that prevent access if T&C not accepted

OUTPUT:
- List ALL files and code sections related to T&C
- Explain what each part does
- Mark:
  [SAFE TO DELETE]
  [REQUIRES MODIFICATION]
  [CRITICAL DEPENDENCY]

DO NOT MODIFY CODE YET.

---

PHASE 2: SAFE REMOVAL

1. Remove:
   - T&C modal/component
   - Acceptance UI elements
   - Unused styles

2. Modify:
   - Login flow:
     - Remove requirement to accept T&C
     - Ensure user proceeds directly after login
   - Routing guards:
     - Remove checks for T&C acceptance

3. Backend:
   - Keep database fields temporarily (DO NOT delete immediately)
   - Stop using them in logic

---

PHASE 3: CLEANUP

1. Remove:
   - Unused variables
   - Unused API calls
   - Dead conditions related to T&C

2. Ensure:
   - No broken imports
   - No unused state variables

---

PHASE 4: VALIDATION

Test and confirm:
- Login works in ONE click (no T&C interruption)
- Dashboard loads immediately
- No console errors
- No blank screens or blocked routes

---

OUTPUT FORMAT:

1. T&C SYSTEM OVERVIEW
2. FILES REMOVED
3. CODE MODIFICATIONS
4. FINAL LOGIN FLOW
5. CONFIRMATION (APP STILL WORKS)

---

IMPORTANT:
- Do NOT break authentication
- Do NOT remove unrelated modals
- Do NOT delete database fields unless explicitly safe
- Make minimal, safe changes only
