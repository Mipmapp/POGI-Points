My deployed application has a Google OAuth login issue that only happens after deployment.

Issue:
- Google Sign-In completes successfully.
- The browser redirects to:
  https://ssaam.vercel.app/?gc=<authentication_code>
- Immediately after that, the application redirects back to the homepage without logging the user in.
- The user session is never created, and I cannot access protected pages.

I need you to perform a complete root-cause analysis and fix the issue.

Tasks:

1. Trace the complete Google authentication flow from the login button to the final authenticated session.

2. Find where the `gc` query parameter is generated and where it is supposed to be processed.

3. Verify that the frontend correctly:
   - Detects the `gc` parameter.
   - Sends it to the backend.
   - Waits for authentication to complete.
   - Stores the returned JWT/session/cookies.
   - Updates the authentication state.
   - Redirects only after successful authentication.

4. Verify that the backend:
   - Receives the `gc` parameter.
   - Exchanges it successfully with Google (if applicable).
   - Creates the user account if necessary.
   - Generates the session/JWT.
   - Returns the correct response.

5. Compare local development vs. deployed (Vercel) behavior and identify exactly where the flow breaks.

6. Check all environment variables and report any missing or incorrect values.

7. Verify:
   - Google Client ID
   - Google Client Secret
   - Redirect URI
   - Base URL
   - Session Secret
   - JWT Secret
   - API URL

8. Check whether cookies are blocked because of:
   - SameSite
   - Secure
   - Domain
   - Path

9. Verify CORS configuration if the frontend and backend are on different domains.

10. Search the entire project for:
   - `gc`
   - `google`
   - `callback`
   - `oauth`
   - `login`
   - `redirect`

11. Look for any code that redirects to `/` before authentication is finished.

12. Check browser console and network requests for authentication failures.

13. If authentication fails, identify the exact file, function, and line responsible.

14. Fix the issue without removing existing functionality.

15. Explain:
   - The root cause.
   - Which files were modified.
   - Why the fix works.
   - Any deployment configuration changes required.

Do not guess or apply temporary workarounds. Trace the complete authentication flow and resolve the actual cause.