Added for debugging: GET session endpoint should return session including rfidScanner. The repository's backend file SSAAM_VERCEL_BACKEND.js must be deployed to the server to apply the changes.

Note: To expose the session via API, we added a GET handler in the backend. If you're using Vercel, commit and push the updated `SSAAM_VERCEL_BACKEND.js` and redeploy.
